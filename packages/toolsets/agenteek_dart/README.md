# Agenteek Dart Toolset 🎯

A toolset for **Agenteek** that gives agents the ability to analyze and interact with Dart code and projects.

## 🧰 Tools Provided

- **Analyze**: Run `dart analyze` on files or directories.
- **Format**: Run `dart format` on Dart source files.
- **Get Pubspec**: Retrieve the contents of the `pubspec.yaml` file.

## 📦 Integration

Add to your `pubspec.yaml`:
```yaml
dependencies:
  agenteek_dart:
    path: ../toolsets/agenteek_dart
```
